# =====================================================================
# Servidor FTP  —  VLAN 100 (Servidores DC, 10.30.0.8/29)
# Acesso do Departamento de Servicos de Informatica (Ed.A).
# Descobrir a placa de rede primeiro com:  ip a
# =====================================================================

sudo ip addr add 10.30.0.12/29 dev ens3
sudo ip link set ens3 up
sudo ip route add default via 10.30.0.9
