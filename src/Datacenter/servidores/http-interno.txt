# =====================================================================
# Servidor HTTP Interno  —  VLAN 100 (Servidores DC, 10.30.0.8/29)
# Acesso restrito a Professores (Ed.A e Ed.B) e Servicos Academicos (Ed.B).
# Descobrir a placa de rede primeiro com:  ip a
# =====================================================================

sudo ip addr add 10.30.0.11/29 dev ens3
sudo ip link set ens3 up
sudo ip route add default via 10.30.0.9
